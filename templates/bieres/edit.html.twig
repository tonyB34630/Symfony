{% extends 'base.html.twig' %}

{% block title %}Edit Bieres{% endblock %}

{% block body %}
    <h1>Edit Bieres</h1>

    {{ include('bieres/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('index') }}">back to list</a>

    {{ include('bieres/_delete_form.html.twig') }}
{% endblock %}
